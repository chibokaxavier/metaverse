import React from "react";

interface NewFeature {
  imgUrl: string;
  title: string;
  subtitle: string;
}

const NewFeature = ({ imgUrl, title, subtitle }: NewFeature) => {
  return <div className="flex-1 flex flex-col sm:max-w-">

  </div>;
};

export default NewFeature;
